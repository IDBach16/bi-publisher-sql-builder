# Secrets
.env
.env.*
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.venv/
venv/

# JDBC driver — 81 MB, exceeds GitHub's 100 MB hard cap and 50 MB warning
orfujdbc-*.jar
*.jar

# Client BI Publisher data models — committed because the repo is PRIVATE
# and the RAG Catalog Library tab needs them in production.
# If this repo ever goes public, re-add the excludes:
#   *.xdm.catalog
#   Data Models.catalog

# Local sandbox / scratch
procurement_test_scenarios.sql

# Debug assistant runtime log (export from the Debug tab to feed back)
debug_history.jsonl

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

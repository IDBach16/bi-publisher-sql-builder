streamlit>=1.30.0
anthropic>=0.40.0
pandas>=2.0.0
openpyxl>=3.1.0
python-dotenv>=1.0.0

# Optional AI providers — enable validating/using OpenAI and Gemini keys.
openai>=1.0.0
google-generativeai>=0.8.0

# Live Fusion JDBC bridge (local-only). Uncomment if running where a JVM is
# available. Streamlit Community Cloud has no JVM, so leave commented for
# cloud deploys. The app degrades gracefully when these are missing.
# jpype1>=1.5.0
# JayDeBeApi>=1.2.3
# pyperclip>=1.9.0
